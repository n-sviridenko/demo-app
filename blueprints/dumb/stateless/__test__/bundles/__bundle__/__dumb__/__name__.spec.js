import React from 'react';
import <%= pascalEntityName %> from 'bundles/<%= bundlePath %>/<%= entityPath %>/<%= pascalEntityName %>';

describe('(Component) <%= pascalEntityName %>', () => {
  it('should exist', () => {

  });
});
